server {
    listen 80;
    charset utf-8;
    server_tokens off;

    add_header X-Frame-Options "SAMEORIGIN";

    location / {
        proxy_set_header  Host $host;
        proxy_pass        http://backend-python:5000;
        proxy_redirect    off;
    }
}
